#pragma once
#include <array>

namespace Day18
{
auto TEST_INPUT =
    std::array<std::string, 10>{{"set a 1", "add a 2", "mul a a", "mod a 5", "snd a", "set a 0",
                                 "rcv a", "jgz a -1", "set a 1", "jgz a -2"}};
}
